{
  "name": "@bgp-report/server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "tsnd -r tsconfig-paths/register --inspect=0.0.0.0:9229 --quiet --respawn --transpile-only --ignore-watch node_modules --no-notify src/server.ts",
    "lint": "eslint 'src/**' --fix"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@bgp-report/eslint-config": "^1.0.0",
    "ts-node-dev": "^1.0.0",
    "tsconfig-paths": "^3.9.0"
  }
}
